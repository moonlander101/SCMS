# Ports (you can still use these separately if needed)
USER_SERVICE_PORT=8001
LOGISTICS_SERVICE_PORT=8002
GATEWAY_PORT=8000

# Full URLs — write them explicitly
USER_SERVICE_URL="http://127.0.0.1:8001"
LOGISTICS_SERVICE_URL="http://127.0.0.1:8002"

# Shared JWT secret
JWT_SECRET=my_very_secret_key
